[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_20"]

[deploy]
startCommand = "npm install && node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3